/*
 * Specifies how admin activity should be relayed to users.  Add up the values
 * below to get the functionality you want.
 * 1: Show to triggering client
 * 2: Show to target client
 * 4: Log Action to sourcemod logs
 * 8: Print action to server console
 * 16: Show to all players (Follows sm_show_activity rules) (automatically includes 1+2)
 *
 * NB: Not all commands available through super commands follow these rules currently. 
 * Global commands (like map extend or shutdown) currently always print to everyones chat.
 */

Overrides
{
	"default"	"8"
	/*
	"sm_exec"	""
	*/

}